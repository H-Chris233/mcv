# Android Architecture

Status: placeholder for Android implementation.

The intended Android architecture is Compose UI, ViewModel and StateFlow state, repository interfaces for NFC, Keystore, and vault storage, and UniFFI calls into Rust for protocol behavior.

M0 contains only a launchable Compose app scaffold.
